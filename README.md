# gulp-starter

``` bash
# Установка зависимостей
$ npm install

# Запуск локального сервера на localhost:3000
$ npm run dev

# Запуск сборки билда в папку build
$ npm run build

# Публикация билда на GitHub Pages
$ npm run deploy
```
